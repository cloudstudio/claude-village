{
  "name": "agent-village",
  "version": "1.0.0",
  "description": "3D Minecraft-style visualization for Claude agent work",
  "type": "module",
  "bin": {
    "agent-village-mcp": "./server/mcp.js"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "mcp": "node server/mcp.js --standalone",
    "start": "concurrently \"npm run dev\" \"npm run mcp\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "three": "^0.182.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "vite": "^5.0.0"
  }
}
